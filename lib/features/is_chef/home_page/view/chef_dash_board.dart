import 'package:flutter/material.dart';

class ChefDashBoard extends StatelessWidget {
  const ChefDashBoard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("CHEF APP")),
    );
  }
}
